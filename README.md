# url-shortener
A Python URL shortening system that converts long URLs into short unique codes and retrieves them efficiently. The project uses hashing, dictionaries, and file handling to simulate real-world URL shortening services with fast lookup and persistent storage.
